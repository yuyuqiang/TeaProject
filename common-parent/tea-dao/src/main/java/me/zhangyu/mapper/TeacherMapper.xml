<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.zhangyu.mapper.TeacherMapper">

    <select id="findTotalRecords" resultType="int" parameterType="int">
        SELECT COUNT(*) FROM t_tea
    </select>

    <select id="findTeachersWithPage" resultType="teacher" parameterType="int">
        SELECT * FROM t_tea LIMIT #{startIndex},#{pageSize}
    </select>

    <select id="addTeacher" resultType="teacher" parameterType="me.zhangyu.model.Teacher">
        INSERT INTO t_tea (teaId,teaNum,teaRealName,teaSex,teaAge,loginName,loginPwd,del) values(null,#{teacher.getTeaNum()},#{teacher.getTeaRealName()},#{teacher.getTeaSex()},#{teacher.getTeaAge()},#{teacher.getLoginName()},#{teacher.getLoginPwd()},null)

    </select>


</mapper>